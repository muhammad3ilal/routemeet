# Copy to .env if you want to override the defaults.
# All of these are optional -- the app works out of the box using
# the free public Nominatim and OSRM demo servers.

PORT=4000

# Point these at self-hosted instances if you outgrow the public
# demo servers' rate limits.
# NOMINATIM_BASE_URL=https://nominatim.openstreetmap.org
# OSRM_BASE_URL=https://router.project-osrm.org
